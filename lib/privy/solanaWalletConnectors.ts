export const solanaWalletConnectorOptions = {
  // Keep the user's main Solana wallet connected after login so it is available
  // to sign swaps that use that wallet's balances.
  shouldAutoConnect: true,
};
